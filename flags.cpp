#include <iostream>
#include <numeric>

using namespace std;

double AddNumbers(double, double);

int main(int argc, char** argv) {

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cout.setf(ios::boolalpha); // set bool output to 'true' and 'false' instead of '1' and '0' respectively

    return EXIT_SUCCESS;

}
